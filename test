简单聚合：同一节点，多个响应数。
多路选择：提交后，有多条流程走向，可以定义成手动选择或条件正确选择。
多路聚合：多个流程走向最后走向同一个节点。（即把几条路合并）
同步聚合：即多个流程走向都到达后执行才能往后面进行。
路径鉴别：到从一个节点有多条流出流向时，工作流可以根据线上定义的条件自动选取确定的路走。
任意循环：流程可以在有形成的圈状的走向时，可以任意循环，只有当有条件正确的出路时，才停止循环。
绝对终止：流程可以在任意节点直接跳转到流程结束，使整个流程上的全部任务终止。
非同步多实例：在节点上配制多实例组时，多实例组中的所有人都可以从这个节点后各走各的路，形成多个相同的分支路。
